/**
 * Idle state for the account's @drawer slot: render nothing. This is what shows
 * on the account page itself and on any hard navigation to a tool route (so the
 * full tool page renders as `children` while the drawer stays empty).
 */
export default function DrawerDefault() {
  return null;
}
